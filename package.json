{
  "name": "api-test",
  "version": "1.0.0",
  "description": "api-test",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "newman": "^4.1.0",
    "newman-reporter-html": "^1.0.2",
    "reporter": "^0.1.0"
  },
  "scripts": {
    "test": "newman run tests/test.json --reporters html",
	"test1": "newman run tests/test.json junit --reporter-junit-export newman.xml"
  },
  "author": "P Aleksandrov"
}
