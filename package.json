{
  "name": "api-test",
  "version": "1.0.0",
  "description": "Postman api test",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "newman": "^4.1.0",
    "reporter": "^0.1.0"
  },
  "scripts": {
    "test": "newman run tests/test.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "P Aleksandrov"
}
